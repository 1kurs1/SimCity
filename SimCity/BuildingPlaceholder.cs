namespace SimCity
{
    public class BuildingPlaceholder : Building
    {
        public BuildingPlaceholder(int width, int height, string spritePath, int x, int y)
        {
            Width = width;
            Height = height;
            SpritePath = spritePath;
            X = x;
            Y = y;
        }
    }
}